// Question data - loaded from JSON for easy editing
import data from './questions.json';

export const questions = data.questions;
export const questionCategories = data.questionCategories;
export const exams = data.exams;
export const timingConfig = data.timingConfig;

export default questions;
